import React from 'react';
import { Component } from 'react';
import LoginForm from '../../containers/auth/login_form';
export default class Login extends Component {
    render() {
        return (
            <LoginForm/>
        );
    }
}